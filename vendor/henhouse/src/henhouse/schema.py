"""JSON schema identifiers for henhouse envelopes.

These strings are the contract. Field lists inside a version can still move;
treat breakage as expected until a later version is frozen.
"""

SCHEMA_SESSION = "henhouse.session.v1"
SCHEMA_TOOLS = "henhouse.tools.v1"
